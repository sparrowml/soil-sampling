import ReactDOM from 'react-dom';
import { useState, useRef, useEffect } from 'react';

import Mapbox from './Mapbox';


const App = () => {

  return(
    <div>
      <Mapbox/>
    </div>
  );
};

export default App;